// Notación
// ; -> delimita el final de una linea,ya no es necesario, pero por buenas practicas
const b = 4 

//. -> se usa en los objetos para acceder a los atributos

//[] -> listas, arreglos, o arrays
const ar= [1,2,3,4]
console.log(ar[2])
//() -> Funciones
function suma(a,b) {
    // se escribe la funcion
}

//{} -> LLaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5, 
    altura:10
}

if (true) {
    // todo lo que hay en la llaves
}

